{
  "name": "patchlings",
  "version": "0.1.0",
  "private": true,
  "description": "Give your coding agent a soul — watch it work, learn as it patches.",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.11.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "demo": "pnpm --filter @patchlings/runner demo",
    "dev": "pnpm --filter @patchlings/runner dev",
    "run": "pnpm --filter @patchlings/runner run --",
    "replay": "pnpm --filter @patchlings/runner replay --",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}